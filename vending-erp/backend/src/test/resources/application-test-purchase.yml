# M1-4 采购模块集成测试专用:独立 vend_test_purchase 库(vend-mysql 容器 3308),
# Flyway 自动迁移,与 vend_dev / vend_test(别的模块在用)完全隔离。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_purchase?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123
