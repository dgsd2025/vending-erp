# M1-6 报表/加权成本引擎集成测试专用:独立 vend_test_report 库(vend-mysql 容器 3308),
# Flyway 自动迁移;与 vend_dev / vend_test / vend_test_imports 完全隔离。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_report?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123

vend:
  import:
    storage-dir: target/test-report-storage
