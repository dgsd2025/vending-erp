# M3-5 资金调整单+钱盘三核对集成测试专用:独立 vend_test_cashadj 库(vend-mysql 容器 3308),
# Flyway 自动迁移,与 vend_dev / 其他并行票测试库完全隔离。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_cashadj?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123

vend:
  attach:
    storage-dir: target/test-cashadj-attachments
