# M2-3 Pre-kit 配货单闭环集成测试专用:独立 vend_test_prekit 库(vend-mysql 容器 3308),
# Flyway 自动迁移,与 vend_dev / 其他模块测试库完全隔离。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_prekit?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123

# 导入归档到 target 下,clean 时一起清
vend:
  import:
    storage-dir: target/test-prekit-storage
