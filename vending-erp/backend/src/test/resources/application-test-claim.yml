# M3-4 索赔/支出/线下复合单集成测试专用:独立 vend_test_claim 库(vend-mysql 容器 3308),
# Flyway 自动迁移(含 V1.0.9),与 vend_dev / 其他并行票测试库完全隔离。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_claim?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123

# 凭证归档写 target 下临时目录,测试后随 clean 清理
vend:
  attach:
    storage-dir: target/test-attachments-claim
