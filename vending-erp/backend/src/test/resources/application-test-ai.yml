# M4-5 AI 接入点全家桶集成测试专用:独立 vend_test_ai 库(vend-mysql 容器 3308),
# Flyway 自动迁移(含 V1.0.16 白名单视图);与 vend_dev / 其他 vend_test_* 完全隔离。
# llm.* 三项全空 → RoutingLlmService 走 MockLlmService,断言零外呼(OpenAiCompatLlmService.requestCount==0)。
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/vend_test_ai?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: vend123
llm:
  base-url: ""
  api-key: ""
  model: ""
