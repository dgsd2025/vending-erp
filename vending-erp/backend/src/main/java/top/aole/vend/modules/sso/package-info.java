/**
 * SSO 模块(留位,未实现)。
 *
 * 按 ole 平台规范,后续接澳乐门户单点登录(auth_code 换 token 流程)。
 * 开关:application.yml 里 sso.enabled=false。
 * 实现时按 DDD 分层建 interfaces / application / domain / infrastructure 子包。
 */
package top.aole.vend.modules.sso;
