# 本机部署 override:给 vend-web 开一个宿主端口(ECS 上走 central-caddy 反代不需要)
# 用法:docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.local.yml up -d
services:
  vend-web:
    ports:
      - "8089:80"
