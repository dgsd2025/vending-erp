# 售卖机 ERP · 全自动开发提示词(复制粘贴即用)

## ⭐ 推荐版(逐里程碑全自动,每段收尾给报告)

```
按 vending-erp-dev skill 全自动开发智慧园区售卖机 ERP。

目标:完成【冲刺 0 + 里程碑 1(数据地基)】,一口气模式,中途不问我,熔断才停。

规格来源(不要凭记忆,先读):
- ~/系统开发/智慧园区/调研报告-园区自动售卖机管理系统.md(v1.8,§13 修正案效力最高)
- ~/系统开发/智慧园区/UI-mockup/index.html(V15 = UI 验收标准)
- ~/系统开发/智慧园区/穿行审计报告-断点卡点逻辑错误.md

硬验收(全过才算完成):
1. 用 小卖铺数据表8.4 的真实数据跑通导入+别名归集,重算 6-7 月毛利与老 Excel《销售毛利表》对平(误差 <1%,逐 SKU 出对照表给我)
2. 穿行审计 14 场景写成自动化回归测试,P0 相关场景全绿
3. 每个 ticket 完成即真测留证据;每个里程碑末跑 code-reviewer 盲审 + skill 七律审计
4. UI 与 mockup V15 逐页对照,布局风格一致

外部依赖处理(留给我,不阻塞你):
- AI 模型 API key:统一走 OpenAI 兼容网关 + 环境变量占位,开发期用 mock 返回,最后我注册真 key 换上
- 厂家后台自动下载:需要账密的部分做成可配置脚本框架,真实凭据最后我填
- 部署上线:只开发+本地跑通,部署方案单独列文档等我拍板
- 所有留给我的事,收尾时汇总成《待办清单-需要老板做的事.md》

断点续做(上下文防满机制,最重要的一条):
- 开工第一步:若存在 TODO.md 则读它+git log 续做,不存在才从头拆 ticket 建 TODO.md
- 每完成一个 ticket:更新 TODO.md 状态 + git commit + 真测证据记入 PHASE5-VERIFICATION-LOG.md
- 大块代码生成派给 subagent 干(隔离上下文),主窗口只当调度员
- 感觉上下文吃紧就主动收尾:提交进度+更新 TODO+输出一句"下个窗口发同样提示词继续"

工程约束:本地 Docker 起 MySQL;git 微提交;新分支开发不碰 main;
熔断条件(停下来问我):要花钱 / 要对外发布 / 规格有矛盾且 §13 也裁决不了 / 毛利对不平查不出原因。

完成后给我:完成报告 + 真测证据 + 对照表 + 下一里程碑建议。
```

之后每段只改第二行:【里程碑 2(补货闭环)】→【里程碑 3(钱账)】→【里程碑 4(BI/PDCA/AI)】。

## 为什么这样质量有保障(你不用担心提示词水平)

1. **规格不在提示词里,在文档里**——提示词只是指针。v1.8 报告 13 章 + mockup 17 页 + 穿行审计,比任何提示词写得都全。
2. **质量靠四道自动闸门,不靠 AI 自觉**:①老 Excel 毛利对平(数据正确性的铁证)②14 场景回归测试(逻辑闭环)③每 ticket 真测+盲审 ④里程碑末七律审计。任何一道不过,不算完成。
3. **逐里程碑而不是一句话全干完**:每段收尾你花 10 分钟看报告,风险被切成 4 段;一次干 3 个月才是烂尾配方。
4. **外部事项"占位+最后换真"**:mock 网关/环境变量/凭据配置框架,开发全程不被你的注册进度阻塞。

## 无人值守模式(外出用:一个主窗口,回来看到完成)

外出前三步:
1. **放权限**:会话切到"自动接受编辑和命令"模式(否则第一个权限弹窗就卡死全程)
2. **发循环指令**(代替直接发提示词):

```
/loop 按 ~/系统开发/智慧园区/开工提示词.md 推荐版执行智慧园区售卖机 ERP 开发,
每轮:读 TODO.md 续做若干 ticket → 更新 TODO + git 提交 + 真测留证。
重活派 subagent 并行。里程碑验收四道闸门全过 → 停止循环输出完成报告;
触发熔断(要花钱/对外/规格矛盾/毛利对不平) → 停止循环写明卡点等我。
```

3. **走人**。回来看两样东西:完成报告(或熔断卡点)+ TODO.md 进度。
安全兜底:只动项目目录+新分支、git 每 ticket 可回滚、红线硬停,与权限放开不冲突。

## 备选:极简版(想省事时用,质量闸门由 skill 兜底)

```
按 vending-erp-dev skill,全自动完成冲刺 0 + 里程碑 1,验收=毛利对平老 Excel+14 场景回归绿,
外部依赖 mock 占位留给我,熔断才停,完事给报告。
```
