# 全链路穿行审计报告 · 断点/卡点/逻辑错误

> 2026-08-05 · 对象:调研报告 v1.7 + Mockup V12 · 方法:14 个真实场景(含异常)逐步穿行,走到所有账归零闭合为止
> 结论:**流程闭合度 6/10**——正向主干超配,逆向/异常/跨期三类路径存在 4 断 4 卡 3 错,全部可在 DESIGN_DOC 阶段以"补表字段+口径规则"低成本修复;进代码再修每条都是带数据迁移的返工。

## 一、穿行总表

| # | 场景 | 结论 | 定位 |
|---|---|---|---|
| 1 | 正常补货日 | 通(毛刺) | 配货单↔转移单核销匹配规则未定义 |
| 2 | 拿货日 | **断** | 无采购订单 → 公式里的"在途库存"没有数据生产者 |
| 3 | 月度盘点日 | **断** | 钱盘发现账户差异后无单据可落地(缺资金调整单) |
| 4 | 兑换出库 | **错** | 订单类型未分流 → 污染待结算虚账+销售额,且可能双扣库存 |
| 5 | 故障线下卖 | 卡 | 钱通;货和销量与"机器账后台权威"冲突未定义 |
| 6 | 盘亏索赔到账 | **错** | 索赔应收不在资产公式;赔付收入进不了利润表 |
| 7 | 新商品后台先卖 | 卡 | 无采购史成本口径缺失;负库存拦截与导入放行两处矛盾 |
| 8 | 新机首次铺货 | **错** | 手工铺货单+后台补货记录导入 → 同一次上架双扣仓库 |
| 9 | 淘汰清仓剩货 | 卡 | "归零才停售"与仓库残余互为死锁 |
| 10 | 红冲连锁 | **断** | 已售货/加权成本/已付款三条连锁全空白,红冲还会被负库存拦截卡死 |
| 11 | 漏导3天/乱序导入 | 通(毛刺) | 增量推算需按业务时间戳而非导入顺序 |
| 12 | 后台改价未登记 | 卡 | 毛利按实收算的口径未写死;改价侦测缺失 |
| 13 | 供应商切换期 | 通(毛刺) | deduction 缺 supplier_id,抵扣会串户 |
| 14 | 锁账后补导漏单 | **断** | 锁账只管改单不管补导入,追改已归档报表无解法 |

## 二、开发前必修 6 件(P0,全是表结构+规则层面)

1. **红冲连锁规则集**:数量错→整单红冲(仅限无下游动作);单价错→新增"成本调整单"(未售部分调存货金额、已售部分进利润表"成本调整"行,不追溯);已付款→生成应付红字(settle_bill 加 direction 字段)冲下一单;红冲免负库存拦截但强制"影响清单"确认页(库存/应付/毛利各变多少)。
2. **锁账×补导期间归属**:sale_record 加 `biz_period`(业务归属月)+`book_period`(入账月);锁账后补导 → 已归档报表永不重算,当月利润表加"上期调整"行承接;已核销结算单只展示"锁后新增差异"提示不改状态。
3. **order_type 分流三口径**:①待结算虚账/结算对账口径=仅"正常"(退款为负);②补货日均销量口径=正常+兑换(真消耗库存),不含测试;③毛利=实收金额−加权成本,兑换收入按0、成本由补贴到账对冲。硬规则:后台已有的出货**禁止再手工录出库**(保存时按机器+SKU+日期碰撞检查)。
4. **转移单唯一生产者=后台补货记录导入**;手工转移单一律"预挂单"状态,导入时按机器+SKU+当日窗口自动匹配冲抵,差量记带回;超48h无后台记录才转正。首铺流程改为"去后台执行补货→次日导入自动生成铺货单"。
5. **轻量采购订单表** purchase_order(草稿级不进账):供应商/明细/预计到货日;在途=Σ(订单量−已收量);收货单从订单带"应收"列;超期未到黄灯。
6. **公式补漏**:资产快照 += 索赔应收(claim 申请中);利润表加"其他收入"行(赔付/平台外/补贴),cash_flow 类别与利润表行建一一映射;损耗报表净损耗=损耗−已获赔。

## 三、上线前必修(P1)

7. **资金调整单**(doc_type 新增):钱盘差异的唯一出口(账户/金额/原因枚举/老板确认→生成流水);应付核对不符出口=补录或红冲两个按钮。
8. **新SKU成本口径**:product 加"参考成本";无采购史毛利显示"—(成本待补)",禁止用0参与加权;导入生成的转移单遇仓库不足→允许负库存+"待补录采购"红灯(导入豁免拦截,手工单不豁免)。

## 四、管理债(P2)

9. **改价侦测**:导入时校验 sale_record.单价≠product.售价 → 弹确认更新档案+写 price_log(同时喂饱改价留痕和定价 PDCA)。
10. **"清仓中"商品状态**:禁采购/补货建议,允许上架/退货/报损;清仓中仓库>0 超30天 → 三选一提示(退供/报损/换机促销)。
11. **deduction 加 supplier_id**(必填):结算单只允许带入同供应商待抵扣。
12. **prekit_ticket 配货单落表**(状态:已生成/已执行/已核销/有差异):核销=次日转移单匹配,差量自动算带回率(否则补货 PDCA 核心指标无数据生产者)。乱序导入规则:机器库存=最近快照+快照后**业务时间戳**增量。
13. **线下销售复合单**:一次录入同时生成 sale_record(线下补录,不入待结算)+cash_flow(平台外收入)+机内库存差异豁免标记。

## 五、总评

正向单据链+状态机+凭证+PDCA 骨架属超配水平;死穴全在逆向路径(红冲/退货/调整)、异常口径(兑换/线下/新品)、跨期数据(锁账×补导)。**修复成本:约 5 张表加字段 + 2 张新表(purchase_order/prekit_ticket)+ 十几条口径规则,全部落入 DESIGN_DOC 即可,无需改任何已画 UI。**
