# M2-10 盲审修复记录(P0-1 + P1-1/1-2/1-3)

- **修复对象:** `verification/M2-10-盲审报告.md` 中的 P0-1、P1-1、P1-2、P1-3(P2 ×9 不在本票范围)
- **修复日:** 2026-08-06
- **验证:** 全仓 `mvn test` → **Tests run: 212, Failures: 0, Errors: 0, Skipped: 12**(基线 208 + 新增 4;Skipped 12 为既有 @Disabled 的 M3 钱账依赖断言,与本票无关)

---

## P0-1 核销窗口重复计数 → 转移单与配货单 1:1 占用制

- **修法:** 核销不再按"±48h 窗口内全部导入转移单汇总",改为在窗口内**尚未被任何配货单占用**的转移单里按 `biz_date` 距配货日最近取一张,`verify_doc_id` 从"追溯指针"升级为"占用登记"(一张转移单只授信一张配货单),迁移 `V1.0.5` 加唯一索引 `uk_verify_doc` 兜底并发,历史重复占用数据先去重;批量核销时配货日早的单先挑(老单优先占离自己最近的批次)。
- **改动文件:**
  - `vending-erp/backend/src/main/resources/db/migration/V1.0.5__m2_p0_verify_occupy.sql`(新增:历史去重 + uk_verify_doc 唯一索引;V1.0.5 是并行票留下的空号,application.yml 已配 `out-of-order: true`,已有 V1.0.6 的库可补跑)
  - `vending-erp/backend/src/main/java/top/aole/vend/modules/prekit/mapper/PrekitQueryMapper.java`(`importedLoadedByMachine`/`firstImportDocId` 两个窗口汇总查询 → `pickUnoccupiedImportDocId`(NOT EXISTS 排除已占用 + 按 ABS(DATEDIFF) 取最近)+ `loadedByDoc`(单张单按 SKU 汇总))
  - `vending-erp/backend/src/main/java/top/aole/vend/modules/prekit/service/PrekitService.java`(`verifyOne` 改单张占用取数、`verify_doc_id` 落被占用单;`verifyAfterImport` 候选单按 planDate 升序处理)
- **关键回归(本次新增,钉死"日常班次"场景):** `PrekitFlowTest.dailyShiftTwoTicketsVerifyEachAgainstOwnBatch` —— 同机连续两日两张配货单(各带出 10)+ 两批导入(上架 10 / 上架 8):第一张核销到第一批(带回率 0),第二张核销到第二批(上架 8、带回 2、带回率 0.2000、状态「有差异」),两张单 `verify_doc_id` 互不相同。修复前第二张会拿两批总和 18 算 → 带回率恒 0(报告点名的静默 KPI 归零),该测试在旧代码上必红。
- **绿否:** ✅ 绿(PrekitFlowTest 11/11,含原 10 例全过——全配/部分/超窗/幂等语义不变)

## P1-1 盘点确认抢占(并发双确认翻倍造财务单)

- **修法:** `StocktakeService.confirm` 在全部只读守卫(锁账/原因/老板阈值)之后、写副作用(盘盈亏/配对退库/锚点)之前,条件 UPDATE 抢状态:`UPDATE yc_vend_stocktake SET st_status='处理中' WHERE id=? AND st_status='待确认'`(参考 M1 DocStatusGuard 模式),affected≠1 → 明确报「已被处理…请刷新查看结果」;"处理中"为事务内瞬态,提交覆盖为"已完成"、异常回滚回"待确认"。守卫拒绝(如 ¥50 老板阈值)发生在抢占前,状态不动可重试。
- **改动文件:**
  - `vending-erp/backend/src/main/java/top/aole/vend/modules/stocktake/domain/entity/Stocktake.java`(新增 `ST_CONFIRMING="处理中"`,st_status 列为 varchar(16) 无需迁移)
  - `vending-erp/backend/src/main/java/top/aole/vend/modules/stocktake/service/StocktakeService.java`(confirm 抢占块)
- **并发测试(本次新增):** `StocktakeConcurrencyTest.concurrentConfirmGeneratesFinancialDocsOnlyOnce` —— 真双线程(CountDownLatch 闸门,类不加 @Transactional):恰好 1 次成功、1 次报「已被处理」;盘亏单恰好 1 张、仓库账 10−2=8(双确认会变 6);事后串行再确认也被拒。
- **绿否:** ✅ 绿(StocktakeFlowTest 既有 11 例同时全过——⑥老板阈值/⑦锁账守卫拒绝后可重试语义保持)

## P1-2 冤枉红灯(补物化过去日直接判逾期)

- **修法:** `TaskService.markOverdue` 定罪前先对每条过去日待办实例跑**对应日期**的 `runCheck(checkType, taskDate)`:过了按"系统校验✅"补完成(done_type=系统校验、done_by=系统),没过才标逾期——逾期与完成用同一把尺子;返回值改为"实际标逾期数"。
- **改动文件:** `vending-erp/backend/src/main/java/top/aole/vend/modules/task/service/TaskService.java`(markOverdue)
- **测试(本次新增):** `TaskEngineTest.backfilledPastDayRunsOwnDayCheckBeforeOverdue` —— 昨日真有成功 import_batch(create_time 回拨昨日)→ weekView 补物化昨日实例后必须是绿灯(STATUS_DONE + DONE_AUTO),不是红;对照组(昨日无证据的 REPLENISH 每日任务)仍标逾期,证明尺子没放松。既有「④ 逾期红灯」用例(昨日无证据→红)同时保持绿。
- **绿否:** ✅ 绿(TaskEngineTest 10/10)

## P1-3 零销量 SKU 进慢销建议(窗口内 0 销量 SKU 从建议消失)

- **修法:** `ReplenishEngine.insertPurchaseSide` 仓库侧循环从"仅 snap.global(窗口内有销量)"改为遍历**在售 SKU 全集**;查不到统计的用 `DemandStatsService.buildStats(空序列)` 构造 avgDaily=0/σ=0 的 DemandStats(isSlow=true)走慢销 min/max 分支;仓库现存批量查询同步改为按在售全集取数。清仓中/停售天然不在集合内,排除语义不变。
- **改动文件:** `vending-erp/backend/src/main/java/top/aole/vend/modules/replenish/service/ReplenishEngine.java`(insertPurchaseSide)
- **测试(本次新增):** `ReplenishEngineIntegrationTest.zeroSalesOnSaleSkuStillGetsSlowMoverSuggestion` —— 28 天零销量在售品(箱规 12、仓库 0):采购建议出现,avgDaily=0、SS=NULL(不套公式)、补到 max 1 箱=12、formulaJson 含"慢销";同场景零销量的清仓中/停售品一行不出。既有慢销/清仓过滤/幂等用例(ReplenishEngineIntegrationTest + Scenario09/15/17)全过。
- **绿否:** ✅ 绿(ReplenishEngineIntegrationTest 8/8)

---

## 全仓总数

```
mvn test(JDK 17,-s settings.xml)
Tests run: 212, Failures: 0, Errors: 0, Skipped: 12   → BUILD SUCCESS
```

- 新增 4 例:PrekitFlowTest ×1(P0-1 连班回归)、StocktakeConcurrencyTest ×1(P1-1 真并发)、TaskEngineTest ×1(P1-2 冤枉红灯)、ReplenishEngineIntegrationTest ×1(P1-3 零销量兜底)。
- 部署提醒(§4.16):V1.0.5 含 `ALTER TABLE ... ADD UNIQUE KEY`,dev/prod 库 Flyway `out-of-order: true` 会自动补跑;若目标库存在同一转移单被多张配货单占用的历史数据,迁移内的去重 UPDATE 会先清后建索引,被清占用的单可用「手动核销」按新 1:1 规则重算。
