# M1-2 档案模块 · 验收证据

> 日期:2026-08-06 凌晨 · 验收人:AI 开发工程师(M1-2)
> 范围:basedata 模块(商品 / SKU别名+待绑队列 / 供应商 / 机器与货道 / 补货参数)后端四层 + 前端 Settings/Products 两页

---

## 验收一:后端单测(真库 vend_dev @3308)

命令:

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
mvn -s settings.xml test -Dtest='ProductServiceTest,AliasServiceTest,MachineSlotServiceTest'
```

结果(**9/9 全绿**):

```
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0 - in top.aole.vend.modules.basedata.AliasServiceTest
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0 - in top.aole.vend.modules.basedata.ProductServiceTest
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0 - in top.aole.vend.modules.basedata.MachineSlotServiceTest
Tests run: 9, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

覆盖点:
- Product:新建/分页/详情、重复 SKU 拦截、改价写 price_log、编辑写 op_log、三态流转(清仓中记 clearance_since,恢复清空)、**停售≠删除**、非法状态拦截
- Alias:绑定幂等、同键改绑留 op_log、编号+条码全空拒绝(不许按名称绑)、解绑物理删+op_log 留 before 痕、解绑后可重绑、pending 确认写 sku_alias+状态翻转+AI建议采纳来源、重复确认拦截
- Machine/Slot/Config:device_id 唯一拦截、停用=状态非删除、货道批量初始化幂等(二次跑新增 0)+同步 slot_count、绑SKU/解绑/容量、参数 upsert 同行更新 + **op_log 旧值→新值**、非法参数拦截

测试数据 TSTP-/TSTA-/TSTM- 前缀自造,@AfterEach JdbcTemplate 物理清理(含 op_log/price_log)。

---

## 验收二:后端 8082 curl 走查(全过)

启动:`mvn -s settings.xml spring-boot:run -Dspring-boot.run.arguments=--server.port=8082`
统一头:`Content-Type: application/json` + `X-User-Name: %E9%AA%8C%E6%94%B6%E5%B0%8F%E9%82%B1`(=「验收小邱」,后端解码成功,见 op_log)

### 步骤1 建商品

```bash
curl -X POST /api/v1/basedata/products -d '{"skuCode":"SP-M12","productName":"M1-2验收·东鹏特饮500ml","category":"饮料","unit":"罐","boxSpec":24,"refCost":3.40,"refPrice":4.80}'
→ {"code":200,...,"data":{"id":23,"skuCode":"SP-M12",...,"refPrice":4.80,"productStatus":"在售"}}
```

### 步骤2 改售价 4.80→5.00,price_log 有记录

```bash
curl -X PUT /api/v1/basedata/products/23 -d '{...,"refPrice":5.00}'
→ {"code":200,...,"refPrice":5.0000}

curl "/api/v1/basedata/price-logs?productId=23"
→ records: [{id:10, oldPrice:4.8000, newPrice:5.0000, changeSource:"手工", effectDate:"2026-08-06"},
            {id:9,  oldPrice:null,   newPrice:4.8000, changeSource:"手工"}]   # 新建初价 + 本次改价,共2条 ✓
```

SQL 复核:`SELECT * FROM yc_vend_price_log WHERE product_id=23;` → 同上 2 行 ✓

### 步骤3 建机器

```bash
curl -X POST /api/v1/basedata/machines -d '{"machineCode":"M12-T1","machineName":"M1-2验收机","deviceId":"DEV-M12-8888","location":"验收点位"}'
→ {"code":200,...,"data":{"id":5,"deviceId":"DEV-M12-8888","machineStatus":"在线"}}
```

### 步骤4 批量建货道 + 幂等复跑 + 绑SKU

```bash
curl -X POST /api/v1/basedata/machines/5/slots/init -d '{"slotCount":5,"capacity":6}'
→ data:[{slotNo:"01"...},...共5道]                       # 新建5道 ✓
再跑一次同请求 → data:[]                                  # 已存在全部跳过,幂等 ✓
curl -X PUT /api/v1/basedata/slots/21 -d '{"productId":23,"capacity":8}'
→ {...,"slotNo":"01","productId":23,"capacity":8.000}     # 绑SKU+改容量 ✓
```

SQL:`SELECT COUNT(*) FROM yc_vend_slot WHERE machine_id=5;` → 5;`machine.slot_count` → 5(自动同步)✓

### 步骤5 绑别名(编号+条码→SKU,不绑名称)

```bash
curl -X POST /api/v1/basedata/aliases/bind -d '{"aliasCode":"HT-9001","aliasBarcode":"6901234567890","aliasName":"东鹏特饮500ml(后台名)","productId":23}'
→ {"code":200,...,"data":{"id":9,"productId":23,"bindSource":"人工"}}
```

### 步骤6 改补货参数,op_log 有 旧值→新值→改动人

```bash
curl -X PUT /api/v1/basedata/replenish-configs -d '{"machineId":0,"productId":0,"cycleDays":10,...}'   # 15→10
curl "/api/v1/basedata/op-logs?targetType=replenish_config&size=3"
→ {id:71, userName:"验收小邱", action:"改参数",
   beforeJson:'{"scopeType":"全局",...,"cycleDays":15,...}',
   afterJson :'{"scopeType":"全局",...,"cycleDays":10,...}'}                  # 旧值→新值→改动人 全齐 ✓
```

op_log 全链抽查(SQL):id 64-71 覆盖 新建product / 新建machine / 批量初始化货道 / 修改货道 / 绑定别名 / 改参数×2,每条带操作人。✓
(走查后已把全局 R 恢复为默认 15,留痕多一条 op_log。)

---

## 验收三:前端两页真渲染(真浏览器)

- `pnpm build` → `✓ built in 2.93s`,无报错(仅主 chunk >500kB 的常规提示)✓
- `pnpm dev`(5174,vite proxy → 8081,另起一个后端实例在 8081)
- 真浏览器逐页验证(Claude Browser,截图为证):
  1. `/settings` 设置中心:五 Tab 齐(机器与货道/商品/供应商/资金账户/参数与阈值);机器 Tab 显示 curl 建的 M12-T1(货道数 5)✓
  2. 商品 Tab:筛选 chip(全部/在售/清仓中/停售/待绑别名0)+ 搜索 + SP-M12 行(成本3.40/售价5.00)✓
  3. 参数与阈值 Tab:全局参数回显 curl 改后的 R=10(前后端真打通),底部「参数改动留痕」直接展示 op_log 旧值→新值 ✓
  4. 资金账户 Tab:「里程碑3开放」占位 ✓(代码内静态文案)
  5. `/products` 商品档案页:p3 风格列表(chip 行/搜索/编码/参考成本/售价/毛利率/30天销量占位「—」/状态/操作),详情下钻留 M1-6 钩子 ✓
  6. **UI 真写入 E2E**:供应商 Tab 点「＋新增供应商」→ 填 GYS-M12 →保存→ 列表出现;SQL 复核 op_log id=72 `演示用户/新建/supplier` ✓(前端 X-User-Name 头编解码链路通)
  7. 浏览器 console 0 错误 ✓

---

## 验收四:进程清理

- 8082 后端、8081 后端(dev 代理用)、5174 vite dev server 均已 kill(见收尾命令输出)。

---

## 留给后续里程碑的钩子

- alias_pending 确认绑定接口已立好(M1-3 导入模块生产队列数据、绑定后回补 sale_record 由 M1-3 做)
- 商品行「详情 ▸」下钻(M1-6 单品分析)、30天销量/动销标签占位「—」
- 供应商期初应付:编辑态锁死,改动走 M3 调整单;资金账户 Tab M3 点亮
- 机器停用确认文案已提示「撤点先退库(M1-4 单据引擎)」

## 已知取舍(记录在案)

1. **别名解绑=物理删除**:uk_alias 含已删行会挡重绑,故解绑走物理删,op_log 留完整 before_json 追溯(sku_alias 是映射表非流水表,不违反「有流水永不删」)。
2. 经手人 user_id 暂记 0,user_name 取 X-User-Name(中文自动 URL 解码);SSO 接入后替换。
3. 分页拦截器补进 `common/config/MybatisPlusConfig.java`(新增文件,不动他人代码;没有它 selectPage 不生效)。
4. 走查造的演示数据(SP-M12/M12-T1/GYS-M12/别名HT-9001)留在 vend_dev 作前端联调种子;全局参数已还原 R=15。

---

## 收尾命令输出(进程清理)

```
kill <8082后端> <8081后端> <vite dev>
8081: cleared
8082: cleared
5174: cleared
```
