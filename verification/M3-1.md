# M3-1 · 钱账基座(账户 / CashFlowWriter / pl_line / 凭证 / settle.mode)验收证据

> 2026-08-06 · 独立测试库 vend_test_money · 后端 8096 · 前端 5187
> 范围:`modules/money/`(基座与通用件,不含应付链/结算/索赔——M3-2/4/5/6 后续票)+ V1.0.7 迁移(yc_vend_config)+ Settings.vue 资金账户 Tab 点亮

---

## 1. 集成测试:MoneyFoundationTest 10/10 绿 + 全仓回归 222 绿

```
[INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0 - in top.aole.vend.modules.money.MoneyFoundationTest
[WARNING] Tests run: 222, Failures: 0, Errors: 0, Skipped: 12   ← 全仓回归(基线 212 + 本票 10)
[INFO] BUILD SUCCESS
```

覆盖(≥8 达标,共 10 例):

| # | 用例 | 断言要点 |
|---|---|---|
| 1 | 期初只能设一次 | 二次设置抛 BizException,报错指向"资金调整单";建户未给期初→后设一次→再设被拒 |
| 2 | 余额=期初+Σ流水 | 1000−300.50+88.80−12.30=776.00,balanceOf 与列表口同口径;openingLocked=true |
| 3 | 写手包私有(反射) | `CashFlowWriter` 类非 public 且无 public 方法——全系统无绕开单据直改钱账的公开入口 |
| 4 | pl_line 枚举完备性 | 16 类别每个有唯一非空去处;10 个利润表行全部被覆盖;类别中文值不撞;落库 pl_line 与枚举推导一致(维修→杂费) |
| 5 | 凭证路径安全 | `../../../../etc/危险覆盖.png` → 归档物理名 `ATT-uuid.png`(服务端生成),canonicalPath 在存储根内;展示名剥净为 `危险覆盖.png`;`evil.sh` 扩展名拒绝;非法 refType 拒绝 |
| 6 | 凭证关联单据 | settlement:906 传 2 件 → listOf 查回 2;countOf=2(后续票"无凭证不能进已结算"取数口);refId 缺失拒绝 |
| 7 | settle.mode 读写 | 默认 UNSET 带"待核实"横幅+三档说明;设 PLATFORM 后横幅消失;非法值拒绝 |
| 8 | 虚拟账户不可手工收支 | 资金调整打到"平台待结算"→ BizException"不可手工收支";业务流水(销售收款入虚账)放行;真实账户资金调整放行 |
| 9 | 写手校验 | 无 refDocType/refDocId、金额≤0、停用账户 全部拒绝 |
| 10 | 流水查询+聚合 | 分页按账户/类别/期间筛选;pl_line×月聚合:电费20+维修30 → 杂费行支50/净−50,索赔45 → 其他收入-赔付行收45 |

## 2. 8096 curl 真测(vend_dev,V1.0.7 flyway 自动迁移 ✓)

```
POST /v1/money/accounts {"accountName":"微信-M31验收","accountType":"微信","openingBalance":500}
  → {"code":200,"data":1}
GET  /v1/money/accounts
  → [{"id":1,"accountName":"微信-M31验收","openingBalance":500.00,"openingLocked":true,"balance":500.00,...}]
POST /v1/money/accounts/1/opening {"openingBalance":999}
  → {"code":500,"message":"账户「微信-M31验收」期初余额只能设一次(已于 2026-08-06T20:58:30 设定为 500.00);如需修正请走「资金调整单」留痕(M3-5)"}
POST /v1/money/attachments (multipart, filename=../../危险.png, refType=payment, refId=1, attType=转账截图)
  → {"code":200,"data":1};归档落 storage/attachments/202608/ATT-d401357acf6f4763a5f273b0aa8e71a8.png(服务端名)
GET  /v1/money/attachments?refType=payment&refId=1
  → [{"id":1,"attType":"转账截图","fileName":"危险.png",...}]   ← 展示名已剥路径
GET  /v1/money/flows?current=1&size=10
  → {"records":[],"total":0,"size":10,"current":1,"pages":0}   ← 空表结构完好(无写接口,流水只由单据事件产生)
GET  /v1/money/flows/pl-summary → []
GET  /v1/money/settle-mode
  → {"mode":"UNSET","banner":"结算模式待核实:请先追一笔真实交易…","options":[UNSET/PLATFORM/DIRECT 三档]}
```

SQL 佐证(vend_dev):`flyway_schema_history` 有 1.0.7;`yc_vend_config` 表已建;
`yc_vend_account` 2 行(1=curl 建,2=浏览器建);`yc_vend_op_log` 有 新建账户/设置期初余额/上传凭证 留痕(经手人:验收员/演示用户)。

## 3. 前端 build + 浏览器真测(5187 → 8096)

- `pnpm build` ✓(仅既有 chunk>500kB 警告,与本票无关)
- 浏览器:设置中心 → 💳 资金账户 Tab(灰位已换真组件):
  - UNSET 横幅原文显示;结算模式三档 radio(待核实高亮)
  - curl 建的「微信-M31验收」显示 期初 ¥500.00 🔒 / 实时余额 ¥500.00
  - **真建一个账户**:新建 →「现金-柜台备用金」/ 类型 现金(下拉分组:真实 4 类 + 虚拟 2 类带"不可手工收支"说明)/ 期初 200 → 保存 → 列表出现 ¥200.00 🔒 / 实时余额 ¥200.00
  - **console 0 错误**(read_console_messages onlyErrors → 空)
- 期初已锁的行操作列显示"期初已锁"tooltip(指向资金调整单),未锁的显示"设期初"按钮(一次性弹窗带锁定警示)

## 4. 进程清理

- 8096 后端(mvn spring-boot:run)已杀;5187 vite dev server 已停;测试造的凭证文件在 backend/target/test-attachments(随 clean 清理)与 storage/attachments/202608(dev 验证产物,保留无害)。
