# M2-8 回归收口验收:M2 断言全开 + 新增场景15-17 + 全仓 0 失败

- 日期:2026-08-06 · 测试库:vend_test_regression(vend-mysql 3308,Flyway 自迁,每例事务回滚)
- 命令:`export JAVA_HOME=$(/usr/libexec/java_home -v 17) && mvn -s settings.xml test`
- 范围:仅测试代码 + 1 处最小生产实现(清仓残余三选一只读提示接口,先红后修,见 §④);未碰前端

## ① 打开的 M2 断言(@Disabled 摘除,全部转绿)

| 位置 | 断言 | 结果 |
|---|---|---|
| Scenario09 `clearanceProductExcludedFromReplenishPlan` | 同机同销量对照:在售品出建议(机器侧在),清仓中商品机器/采购两侧 0 行(recalc 真跑引擎) | ✅ |
| Scenario09 `clearanceOver30DaysThreeChoicePrompt` | 清仓中+仓库>0+超30天 → 提示带 warehouseQty/daysInClearance/choices=[退供,报损,换机促销]+人话 message;未满30天、仓库=0 均不提示 | ✅ |
| Scenario01(M2-3 时已开)4 例 | prekit 表通道 / 预挂单冲抵不双扣 / 超48h转正 / 配货单核销+带回率 0.125 | ✅ 复跑仍绿 |
| Scenario03 升级新增 `monthlyStocktakeEndToEndViaService` | 原有例只验盘盈亏"单据通道";升级为 StocktakeService 端到端:创建快照→录差异→提交→确认自动生成盘盈+2/盘亏−2 并过账、库存=实盘、差异按加权成本 ±7.00、盘点单终态+双单挂接 | ✅ |

## ② 新增 M2 场景回归(regression/ 下 3 个类,4 例全绿)

| 类 | 例 | 断言数 | 链路 | 绿 |
|---|---|---|---|---|
| Scenario15ReplenishClosedLoopTest | fullReplenishLoopFromSalesToTaskAutoCheck | 30 断言 | 通道1真导销售→recalc(par level 30)→配货单→执行→通道2导入核销(上架25/带回5)→takeback_rate=0.1667→仓库75/机器25 三本账→任务日历「补货巡检」+「数据迁移导入」系统校验自动打勾(done_type=系统校验) | ✅ |
| Scenario16StocktakeClosedLoopTest | anchorSelfHealNextDayInferenceAndLossStats | 21 断言 | 缺锚点推算−4(红灯)→机器盘点(账面如实存负)→账错行只校锚不开单+吞货行退库/盘亏配对(仓库净不变)→锚点自愈 8/9→次日销售增量推算 6/6→损耗统计吞货聚合 | ✅ |
| Scenario17SlowMoverCapacityTest | slowMoverUsesMinMaxNotRsFormula | 8 断言 | 日均1(<1.5)不套公式:SS=NULL、补到 max 1箱=12、formula 标慢销;可用=min 水位的慢销品不出建议 | ✅ |
| Scenario17SlowMoverCapacityTest | machineSuggestionHardCappedBySlotCapacity | 6 断言 | 日均20 货道10:目标水位=容量10、建议=10−机内2=8、建议≤容量硬约束 | ✅ |

## ③ 全仓 mvn test 关键行

基线(收口前):

```
[WARNING] Tests run: 203, Failures: 0, Errors: 0, Skipped: 14
[INFO] BUILD SUCCESS
```

收口后(本票终态):

```
[WARNING] Tests run: 208, Failures: 0, Errors: 0, Skipped: 12
[INFO] BUILD SUCCESS
[INFO] Total time:  24.929 s
```

- 净增 5 例(Scenario15×1 + Scenario16×1 + Scenario17×2 + Scenario03 升级×1),打开 2 例(Scenario09 M2 两断言:Skipped 14→12)
- 回归包分项:Scenario09 `7/7`、Scenario15 `1/1`、Scenario16 `1/1`、Scenario17 `2/2`、Scenario03 `6 跑 2 跳(M3)`、Scenario01 `4/4`

## ④ 最小生产实现(先红后修,唯一一处)

- **红**:先开 Scenario09 三选一断言 → `mvn test-compile` 失败 `cannot find symbol: method clearanceAlerts()`(证据:编译期红)
- **修**:`ReplenishEngine.clearanceAlerts()` 只读方法(清仓中 + 仓库>0 + 超 `CLEARANCE_ALERT_DAYS=30` 天 → 行含 productId/warehouseQty/clearanceSince/daysInClearance/choices=[退供,报损,换机促销]/message;不落库不改状态)+ `GET /v1/replenish/clearance-alerts` 端点(ReplenishController)。选址 replenish 模块:已同时依赖 productMapper 与 stockService,零新增依赖方向
- **绿**:断言全过;全仓 208/0 失败确认无回归

## ⑤ 剩余 @Disabled 清单(12 条,全部 M3-pending,无 M2/M4 遗留)

| # | 位置 | 缺什么 | 挂 |
|---|---|---|---|
| 1 | Scenario03 `cashAdjustGeneratesCashFlow` | 资金调整确认→cash_flow(pl_line=不入利润表-资金调整)+账户余额=期初+Σ流水 | M3 钱账 |
| 2 | Scenario03 `payableMismatchTwoExits` | 应付核对不符两个出口(补录/settle_bill 红字) | M3 结算单 |
| 3 | Scenario04(112 行) | settlement.system_amount 聚合仅 正常/退款(兑换不入待结算虚账) | M3 平台结算 |
| 4 | Scenario04(118 行) | 兑换成本厂家补贴对冲(deduction→结算单抵扣) | M3 钱账链 |
| 5 | Scenario05(90 行) | 线下销售复合单一次录入(sale_record+cash_flow 其他收入+豁免联动) | M3 钱账 |
| 6 | Scenario06(67 行) | 索赔单生命周期(申请中→已到账写 cash_flow→关闭) | M3 索赔单 |
| 7 | Scenario06(73 行) | 资产快照月度生成(净流动资产公式 §13.1) | M3 资产快照 |
| 8 | Scenario06(79 行) | 损耗报表净损耗=Σ盘亏−Σclaim已到账 | M3 报表 |
| 9 | Scenario10(237 行) | 已付款单红冲连带生成 settle_bill 红字冲下一单 | M3 结算单 |
| 10 | Scenario13(67 行) | settle_bill 只许带入同供应商待抵扣(deduction_amount 校验) | M3 结算单 |
| 11 | Scenario14(183 行) | 锁后补导命中已核销结算单区间→lock_diff_note 提示 | M3 结算核销 |
| 12 | Scenario14(189 行) | 简版利润表"上期调整"行渲染 | M3 报表 |
