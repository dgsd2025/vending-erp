# M4-7 验收报告 · M4 回归收口 + 全链路场景(Scenario18-21)

- 角色:M4-7 测试工程师
- 库:`vend_test_regression`(全链路场景)/ 各模块场景沿用自身 `vend_test_*`(Flyway 自迁,每例事务回滚)
- 构建:`JAVA_HOME=17` + `mvn -s settings.xml` + 独立构建目录 `scratchpad/m4-7-target`
- 结论:**全仓 342 例 0 失败 0 错误 0 跳过;Disabled 清单为空;新增 4 场景 6 例全绿;零生产修复**

---

## 1. 全仓回归(mvn test 关键行)

### 基线(改动前)
```
[INFO] Tests run: 336, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
```

### 收口(新增 Scenario18-21 后)
```
[INFO] Tests run: 342, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  28.243 s
[INFO] Finished at: 2026-08-07T14:40:13+08:00
```

总数 336 → **342**(+6):Scenario18×1 + Scenario19×1 + Scenario20×1 + Scenario21×3。

### 新增场景 per-class
```
Tests run: 1, ... - Scenario18BiPdcaClosedLoopTest
Tests run: 1, ... - Scenario19MonthlyPackageConsistencyTest
Tests run: 1, ... - Scenario20AiAllMockZeroCallTest
Tests run: 3, ... - Scenario21DataFreshnessTest
```

---

## 2. Disabled 清单

**空(0 个 @Disabled 注解)。**

- `grep -rEn "^\s*@Disabled" src/test` → 0 命中。
- 唯一文本匹配 `RegressionSupport.java:36` 是 javadoc 注释(说明"M2 断言已全部打开"),非注解。
- M1-M3 遗留的条件禁用断言在各里程碑收口时已全开;M4 未新增任何 @Disabled。
- 全仓 `Skipped: 0`,与"0 Disabled"一致。

---

## 3. 跨零点/日期 flaky 稳定化核查

任务点名的两处历史 flaky **均已在既有代码中稳定化,本轮无需再改**:

- `TaskEngineTest`:`private final LocalDate today = LocalDate.now()` 一次性捕获后全程显式传入
  `taskService.todayView(today)`;"每3天"到期用例已把 `anchorDate` 强制设为 `today`
  使 `(today−anchor)%3==0`(见类内注释)。跨零点不漂移。
- `StocktakeFlowTest`:锚点快照已由"写死 8:00"改为 `LocalDateTime.now().minusHours(1)`
  (行内注释标 "M3-9 稳定性修复:凌晨跑测试时 now<8:00 会翻车")。

新增 4 场景一律采用**抗跨零点写法**:纯逻辑边界锚定固定参考日 `2026-08-20`;
真链路 staleDays 断言就近重算期望值 `DAYS.between(asOf, LocalDate.now())` 再比对,
两次 `now()` 相邻取值,消除边界抖动。

---

## 4. 新增场景断言纲要

| 场景 | 例数 | 断言数 | 主线 |
|---|---|---|---|
| **S18** BI→PDCA 闭环 | 1 | 15 | 四象限判淘汰品 → 全店动销率 40%<目标60% → PDCA 起草选品任务 → 到期回查未见效升级(ST_ESCALATED) |
| **S19** 月报包一致性 | 1 | 9 | 七件套各件 = ReportService/ProfitReportService/AssetSnapshotService/BiService 单独查一致;利润表勾稽 + 净家底公式恒等在包层面再验;BI 机器销售额 = 利润销售收入 |
| **S20** AI 全 mock 零外呼 | 1 | 16 | 六挂点(别名/凭证/解释/异常/NL/PDCA起草)全跑 → OpenAiCompat 零请求 + 每挂点 llm_call_log 落库 + PDCA/别名 idempotent 命中 |
| **S21** 数据新鲜度 | 3 | 15 | ①纯逻辑边界 0/3/4/5 天;②真导入批次业务时间 → dataAsOf 三源 GREATEST → staleDays 超3天判陈旧;③当天新数据 → 水印前移 → staleDays=0 |

---

## 5. 先红后修记录

**无。** 四场景 6 例首次运行即全绿(见 `scratchpad/new-scen-run.log`:`Tests run: 6, Failures: 0`),
随后全仓 342 例复跑亦 0 失败。未触发任何生产代码修复。

关键设计避坑(未成红,提前规避):
- S18/S20 走 `test-regression` profile:base `application.yml` 的 `llm.base-url/api-key` 默认空
  → `LlmProperties.isConfigured()=false` → `RoutingLlmService` 走 `MockLlmService` 断路;S20 首行显式断言
  `isConfigured()==false` 作为 mock 前置门。
- S18 动销率取数用 `YearMonth.now()`,故销售/采购锚 `ym.atDay(1)`,与 BI 四象限同月对齐。
- S21 真导入:仓库垫底采购锚 10 天前、机器补货锚 6 天前(均早于出货批次 5 天前),
  避免 `stockWarehouse` 默认今日时间戳污染 dataAsOf 水印。

---

## 6. 生产修复

**无。** 仅新增 4 个测试文件,未改任何 `src/main` 生产代码、未改前端。
