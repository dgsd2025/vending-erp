# M1-4 采购模块 · 验收证据

> 日期:2026-08-06 凌晨 · 验收人:AI 开发工程师(M1-4)
> 范围:modules/purchase(轻量订货单 P0-5 / 收货入库 doc 承载 / 在途查询 / 采购价历史与比价 / 进价 price_log)后端四层 + 前端 Purchase.vue(对照 mockup p4)

---

## 交付物

后端(`vending-erp/backend/src/main/java/top/aole/vend/modules/purchase/`):
- `domain/entity/PurchaseOrder.java` + `PurchaseOrderItem.java`(状态常量:草稿/已下单/部分到货/已完成/已取消)
- `mapper/PurchaseOrderMapper` / `PurchaseOrderItemMapper`(在途聚合 SQL)/ `PurchasePriceMapper`(价史聚合,从已落账 doc_item 算,不落静态表)
- `service/PurchaseOrderService`(建/改/下单/取消/关闭余量/在途查询/超期黄灯)
- `service/PurchaseReceiptService`(从订货单一键生成收货草稿 expect_qty 带出 / 无订单直录 / 确认入库 / 比价 priceCheck 阈值 20%)
- `service/PurchaseReceiptListener`(吃 DocConfirmedEvent,同事务:qty_received 唯一写手 + 订单状态重算 + 进价变动写 price_log change_source=进价)
- `interfaces/PurchaseController`(/api/v1/purchase/...,写操作经 OpLogService 全留痕)
- 复用不重造:收货入库单 = DocService 的采购入库 doc(一张单两状态承载收货→入库,§9.2);库存过账靠已有 StockPostingListener,本模块零库存写代码;重复确认由 DocStateMachine 天然拒绝。

前端:`frontend/src/api/purchase.ts` + `frontend/src/views/Purchase.vue`(⚡任务来源说明 + ①②③④编号流程条 / 订货单列表在途·超期黄灯 / 收货应收实收两列差异标注 / 无订单快捷录入 / 比价提示内嵌黄灯);`vite.config.ts` 加 `VITE_PROXY_TARGET` 环境变量覆盖(多人并行起服用)。

---

## 验收一:集成测试(自建 vend_test_purchase 库,vend_dev / vend_test 未动)✅

- 新建 `backend/src/test/resources/application-test-purchase.yml` → `jdbc:mysql://127.0.0.1:3308/vend_test_purchase`,Flyway 全量迁移(40 表,installed_on=2026-08-06 01:54:08)。
- `PurchaseFlowTest`(@ActiveProfiles("test-purchase") + @Transactional 每例回滚):

```bash
export JAVA_HOME=$(/usr/libexec/java_home -v 17)
mvn -s settings.xml test -Dtest='PurchaseFlowTest'
→ Tests run: 8, Failures: 0, Errors: 0, Skipped: 0(8/8 全绿)
```

用例覆盖(≥6 要求,实做 8):
1. `placeOrder_inTransitAndExpectQty`:草稿不计在途 → 下单后在途=订购全量;一键生成收货草稿 expect_qty=48 带出
2. `partialThenFullReceive`:应收48实收40 → 在途=8、订单=部分到货、仓库账+40;再收8 → 订单=已完成、在途=0、qty_received=48
3. `duplicateConfirmRejected`:第二次确认 BizException(状态机拒绝),qty_received 与库存均不双写
4. `directReceiptWithoutPo`:无订单直录确认 → 仓库+24,stock_ledger.doc_id 挂单(库存只能被单据改)
5. `priceHistoryAndPriceLog`:3.40→3.80 两笔后,价史 last=3.80/min=3.40/count=2;price_log 自动落 1 条 change_source=进价(3.4000→3.8000)
6. `priceCheckWarnOver20Pct`:+10% 不警 / +30% warn=true / 无采购史 warn=false lastPrice=null
7. `overdueYellowFlag`:expect_date 已过且未收完 → PoVo.overdue=true,在途明细行 overdue=true
8. `cancelAndCloseGuards`:已下单未收货可取消(在途清零);部分到货取消被拒 → 关闭余量后=已完成、在途清零

> 插曲:首跑撞 `ConflictingBeanDefinitionException`(并行 M1-3 会话在 modules/imports 里短暂放了一个重名 PriceLogMapper,几分钟后对方自行删除),复跑即绿,与本票代码无关。

## 验收二:8084 起服 curl 主链路 ✅(vend_dev 库,产品 id=24 SP-M14 / 供应商 id=2)

| 步骤 | 请求 | 结果 |
|---|---|---|
| 下订货单 48件@3.40,预计到货=昨天 | POST /api/v1/purchase/orders | data=1(PO-20260806-001) |
| 下单 | POST /orders/1/place | 200 |
| 在途查询 | GET /in-transit/24 | inTransitQty=48,line.overdue=**true**(超期黄灯) |
| 一键生成收货草稿 | POST /orders/1/receipts | RK-20260806-001 草稿,items expect=48/qty=48/price=3.4 |
| 改实收40+确认 | PUT /receipts/1 + POST /receipts/1/confirm | status=已确认 |
| 订单状态/在途 | GET /orders/1 · /in-transit/24 | poStatus=**部分到货**,received=40,**在途=8(=差值)** |
| 重复确认 | POST /receipts/1/confirm | `{"code":500,"message":"非法状态流转:[采购入库]单据不允许从[已确认]流转到[已确认]"}` ✅幂等拒绝 |
| 比价 | GET /price-check?price=4.20 / 3.60 | +23.5% **warn=true** / +5.9% warn=false |
| 再收剩余8件@4.00 | POST /orders/1/receipts → confirm | 订单=**已完成**,在途=0 |
| 无订单直录 24件@3.50 | POST /receipts → confirm | RK-003 已确认 |
| 价格本 | GET /price-history?productId=24 | 陈老板:last=3.5000/min=3.4/count=3 |

SQL 佐证(vend_dev):
```sql
SELECT doc_id,change_qty,balance_qty,unit_cost FROM yc_vend_stock_ledger WHERE product_id=24;
-- 1|+40|40|3.40 → 2|+8|48|4.00 → 3|+24|72|3.50 → 4|+20|92|4.50   (M1-5 引擎自动过账,链路通)
SELECT old_price,new_price,change_source FROM yc_vend_price_log WHERE product_id=24 AND change_source='进价';
-- 3.40→4.00 / 4.00→3.50 / 3.50→4.50  三条全自动落
SELECT po_no,po_status FROM yc_vend_purchase_order; -- PO-001 已完成 / PO-002 部分到货
SELECT qty_ordered,qty_received FROM yc_vend_purchase_order_item WHERE po_id=1; -- 48/48
```
op_log 同步落:新建订货单 / 下单 / 收货回写已收量(purchase_order_item)/ 收货回写→部分到货 / 确认(doc_head)。

## 验收三:前端 build + 真浏览器走查 ✅

- `pnpm build` 过(Purchase-*.js 23.04kB);`vue-tsc --noEmit` 应用源码 0 错(仅遗留 vite.config `node:url` 缺 @types/node 的历史噪音,非本票)。
- 真浏览器(Chrome @ localhost:5175,proxy→8084)造数走查:
  1. 新建订货单:选陈老板+SP-M14×24@4.50 → **比价黄灯当场浮出「⚠️ 最近价 ¥3.5(同供应商)· 涨 28.6%」** → 保存并立即下单 → 列表出现 PO-002 已下单,在途库存 chip「东鹏特饮500ml ×24」
  2. 点「📥 收货」→ 弹收货框(单号 RK-004·来自订货单 PO-002·应收24/实收24/✓一致)→ 改实收20 → 差异列自动变「−4 按实收」金额联动 90.00 → 确认入库 → 差异确认弹窗 → 确定
  3. 页面刷新:PO-002=**部分到货**(黄),在途 chip **×24→×4**,RK-004=已确认+「实收差异 1 行」chip;详情弹窗应收24/实收20(红) 
  4. console 全程无错误(read_console_messages onlyErrors=空)
- 过程中修的 UI 坑:两个录单弹窗加 `:close-on-click-modal="false"`(误点遮罩会丢一屏录入)。

## 验收四:进程清理 ✅

8084 后端(pid 5087/5122)与 5175 vite 已 kill,`lsof` 复核无监听;并行会话的 8083 后端 / 5174 vite **未动**。测试库 vend_test_purchase 保留(下轮复用,数据每例已回滚为空)。
