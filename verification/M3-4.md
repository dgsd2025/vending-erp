# M3-4 · 索赔(claim)+ 杂费/设备(expense/equipment)+ 线下收入复合单(P2-13)验收证据

> 2026-08-06 · 独立测试库 vend_test_claim · 后端 8098 · 前端 5189(dev 验证台 /money-lab)
> 范围:`modules/claim/`、`modules/expense/`(新)+ V1.0.9 迁移(claim.remark / expense.equip_name 两列)
>       + 前端 ClaimPanel / ExpensePanel / OfflineSaleDialog 三组件(不建整页,p7 归 M3-7 装配)
>       + Stocktake.vue 五步向导第 4 步索赔入口点亮 + Scenario05/06 四条 @Disabled 全部打开
> 基座契约遵守:cash_flow 只经 MoneyPostingEvent 过账(写手包私有,未碰 money/settle 内部、未改 stocktake Java;
>              stocktake_item.claim_id 用条件 UPDATE 只回填该列)

---

## 1. 集成测试:ClaimExpenseOfflineTest 10/10 绿 + 全仓回归 252 绿

```
[INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0 - in top.aole.vend.modules.claim.ClaimExpenseOfflineTest
[WARNING] Tests run: 252, Failures: 0, Errors: 0, Skipped: 4   ← 全仓回归(基线 + 本票 10 + 打开 4 条 Disabled)
```

- 剩余 4 skip 均非本票:Scenario04(兑换抵扣,M3-6)×2 / Scenario14(锁账报表,M3 报表票)×2。
- 与并行票共用 target/ 曾出现编译/死锁竞态 → 终验用 `-Dmaven.build.directory=<scratchpad>/m3-4-target` 独立目录跑,252 全绿。

覆盖(≥8 达标,共 10 例):

| # | 用例 | 断言要点 |
|---|---|---|
| 1 | 索赔应收=Σ申请中 | 500+120.50 → 620.50;到账退出、放弃退出(资产快照 M3-6 取数口 `ClaimService#receivable`)|
| 2 | 到账落流水 | category=索赔到账 → pl_line=其他收入-赔付(P0-6);cash_flow_id 回填;余额=期初+到账;已到账再登记/放弃被拒 |
| 3 | 凭证硬门禁 | 无赔付凭证 receive 抛 BizException(提示含"凭证");传凭证后放行;缺省到账额=索赔金额 |
| 4 | 放弃备注必填 | 空白备注拒绝;放弃后 remark 留痕 + 状态=放弃 |
| 5 | 净损耗=损耗−已获赔 | 盘亏出库 3×3.5=10.5,到账 8 → 净损耗 2.5(§13.1;损耗侧=已确认盘亏/报损单成本额)|
| 6 | 支出单凭证门禁+落流水 | 无凭证确认被拒;电费 20 确认 → 流水 支/电费/杂费行,余额 −20,状态已完成;重复确认被拒 |
| 7 | 买设备进台账 | 设备购置缺设备名拒绝;确认 → equipment 行(购入价/日期/折余=购入价/在用/expense_id)+ 回填 equipment_id;该支出仅 1 条流水(台账不进流水);台账可编辑(报废/折余)|
| 8 | 线下复合单三件套原子 | sale_record(线下补录/OFFLINE-前缀/offline_flag=1/settlement_id 空)+ cash_flow(线下收入→其他收入-平台外 13.2)同事务;账户非法 → 事务标记 rollback-only(同生共死)|
| 9 | 线下口径守卫 | CostEngine 销售额=仅正常单(13.2 不入待结算);机器账不扣(8−正常1=7,差异留盘点豁免)|
| 10 | 盘亏行发起索赔 | 归因=吞货掉货/被盗 且 diff<0 → claim_id 条件 UPDATE 回填;同行重复索赔拒("1 行盘亏只能索赔一次");过期报损归因拒("不可索赔")|

## 2. Scenario05/06 四条 @Disabled 全部打开(vend_test_regression,9/9 绿 0 skip)

```
Tests run: 4, ... - in Scenario05OfflineSaleTest   ← offlineCompositeEntryGeneratesCashFlow 已实测
Tests run: 5, ... - in Scenario06ClaimTest         ← claimLifecycleToCashFlow / assetSnapshotIncludesClaimReceivable / netShrinkageReport 已实测
```

- 场景5:一次录入 → 三件套同事务落地,13.2 进平台外收入,豁免标记标 4 瓶,销售额口径/机器账双守卫依旧;
- 场景6:盘亏归因吞货 → 申请中(明细回填 claim_id)→ 到账 → cash_flow 其他收入-赔付 + 回填流水号;
  索赔应收接口 申请中500→500/放弃→0(M3-6 资产快照直接调);净损耗 10.5−8=2.5。

## 3. 8098 curl 真测(vend_dev,V1.0.9 flyway 自动迁移 ✓)

```
POST /v1/claims {"claimTarget":"厂家","amount":35.00}            → {"data":1}(SP-20260806-001)
GET  /v1/claims/receivable                                        → 35.00
POST /v1/claims/1/receive(无凭证)                                 → 500 "未上传赔付凭证——先传…(refType=claim)"
POST /v1/money/attachments (refType=claim)  → 传凭证
POST /v1/claims/1/receive {"accountId":1,"receivedAmount":30}     → 已到账 · cashFlowId=2
GET  /v1/claims/receivable                                        → 0
GET  /v1/claims/net-shrinkage                                     → {loss:0, compensated:30, net:-30}(dev 库无盘亏单,口径正确)
POST /v1/expenses 设备购置1200"立式冰柜"                            → ZC-20260806-001
POST /v1/expenses/1/confirm(无凭证)                               → 500 "未上传凭证——先传付款截图/发票"
传凭证 → confirm                                                   → 已完成 · isEquipment=true · equipmentId=1 · bookPeriod=2026-08
GET  /v1/equipment                                                → [{立式冰柜,1200,折余1200,在用,expenseId:1}]
POST /v1/offline-sales {机器5,商品23,qty4,13.20,账户1}             → OFFLINE-2085360… · cashFlowId=4 + 豁免提示
GET  /v1/money/flows → CF…-0003 收13.2 线下收入→其他收入-平台外 | CF…-0001 收30 索赔到账→其他收入-赔付 | CF…-0002 支1200 设备购置→杂费
```

SQL 佐证(vend_dev):`yc_vend_sale_record#5140` order_type=线下补录/offline_flag=1/settlement_id NULL;
`yc_vend_claim` SP-…-001 已到账/30.00/cash_flow_id=2;`yc_vend_expense` ZC-…-001 已完成/is_equipment=1/equipment_id=1;
`yc_vend_op_log` 有 发起索赔/索赔到账登记/录入支出单/确认支出单/线下收入复合单 留痕(经手人:验收员)。

## 4. 前端 build + 浏览器三组件真走(5189 → 8098,dev 验证台 /money-lab)

- `pnpm build` ✓(仅既有 chunk>500kB 警告);`pnpm typecheck` 仅 vite.config.ts 两条既有 node 类型错,本票文件 0 错
- **ClaimPanel**:发起索赔(平台/22.5/说明)→ 列表出现 申请中,「索赔应收」chip 实时变 ¥22.50 →
  放弃(备注必填弹窗,"平台拒赔"留痕)→ 已关闭,应收回 ¥0;顶部净损耗 chip「¥0−¥30=¥−30」实时;
  到账登记按钮凭证=0 时置灰+守卫提示(硬门禁前后端双保险,后端路径已由 curl/集成测试验)
- **ExpensePanel**:录入 电费/20/微信-M31验收 → ZC-20260806-002 待确认(不选账户先拦"请选支出账户");
  设备台账行「编辑」→ 折余 1200→1000 保存 → 列表实时 ¥1000.00
- **OfflineSaleDialog**:机器 M1-2验收机 / 商品 M32SKU1·东鹏特饮 / 数量4 / 金额13.20 / 账户微信 →
  「一次录入(三件套落地)」→ 成功 toast(OFFLINE-单号+流水号+豁免提示);SQL 复核 #5141:
  sale_record(线下补录/offline_flag=1)+ cash_flow#8(线下收入→其他收入-平台外 13.20)双双在库
- **Stocktake.vue**:盘点页正常渲染;五步向导第 4 步改为「改进/索赔」,有 归因=吞货掉货/被盗 盘亏行时点亮
  「🧾 发起索赔(挂索赔应收)」按钮(金额预填=Σ|盘亏成本额|,弹 ClaimPanel 带盘亏行 prefill);第 5 步
  "索赔挂应收"chip 转绿"已开放"
- **console 0 错误**(/money-lab 与 /stocktake 两页 read_console_messages onlyErrors → 空)

## 5. 交接与遗留

- `/money-lab` dev 验证台(`components/money/MoneyLabDev.vue` + router DEV-only 路由)为**临时**入口,
  生产构建不注册;M3-7 装配 p7 后删除该文件与路由段。
- ClaimPanel/ExpensePanel/OfflineSaleDialog 均为独立组件,p7 装配(M3-7)直接引入即可;
  凭证上传统一复用 /v1/money/attachments(claim/expense refType)。
- 净损耗损耗侧口径 = 已确认 盘亏出库+报损 单 total_amount(book_period 区间筛),红冲单(已红冲态)自动排除。
- 进程清理:8098 后端、5189 vite 均已杀;测试凭证落 backend/target/test-attachments-claim(随 clean 清)
  与 storage/attachments/202608(dev 验证产物,保留无害);独立回归 target 在 scratchpad,会话结束自清。
